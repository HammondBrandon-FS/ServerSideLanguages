<!-- nav.ejs -->
<nav class="navbar navbar-expand-lg navbar-light" id="mainNav">
    <div class="container px-4 px-lg-5">
        <a class="navbar-brand" href="/index.ejs">Start Bootstrap</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            Menu
            <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ms-auto py-4 py-lg-0">
                <li class="nav-item">
                    <% if(pagename=="Home") {%>
                        <a class="nav-link px-lg-3 py-3 py-lg-4" style="color:orange" href="/">Home</a>
                    <% }else{ %>
                        <a class="nav-link px-lg-3 py-3 py-lg-4" href="/">Home</a>
                    <%}%>
                </li>
                <li class="nav-item">
                    <% if(pagename=="About") {%>
                        <a class="nav-link px-lg-3 py-3 py-lg-4" style="color:orange" href="/About">About</a>
                    <% }else{ %>
                        <a class="nav-link px-lg-3 py-3 py-lg-4" href="/About">About</a>
                    <%}%>

                    <%if(typeof(sess)!="undefined" && sess.loggedin==true){%>
                        <a class="nav-link px-lg-3 py-3 py-lg-4" style="color:orange" href="/profile">Profile</a>
                        <a class="nav-link px-lg-3 py-3 py-lg-4" style="color:orange" href="/logout">Logout</a>
                    <%}%>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-lg-3 py-3 py-lg-4" href="post.html">Sample Post</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-lg-3 py-3 py-lg-4" href="contact.html">Contact</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
<div style="z-index: 1031;
    display: block;
    text-align: right;
    background-color: #333a40;
    margin-right: 0px;
    padding-right: 20px;
    color: gray;
    position: absolute;
    width: 155px;
    margin-top: 4px;
    padding-top: 5px;
    right: 0px;
    border-radius: 10px;
    padding: 11px;
    font-size: 12px;">

    <form action="/login" method="POST">
        <input type="text" name="email" placeholder="email">
        <input type="text" name="password" placeholder="password">
        <input type="submit">
    </form>
</div>

<%if(typeof(errors)!="undefined"){%>
    <%for(x in errors){%>
        <br><span><%= errors[x]%></span>
    <%}%>
<%}%>